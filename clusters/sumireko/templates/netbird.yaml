apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Chart.Name }}-netbird
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/Lichthagel/infra
    path: apps/netbird
    targetRevision: HEAD
  destination:
    server: {{ .Values.cluster.address }}
    namespace: netbird
