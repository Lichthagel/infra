apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Chart.Name }}-unbound
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/Lichthagel/infra
    path: apps/unbound
    targetRevision: HEAD
  destination:
    server: {{ .Values.cluster.address }}
    namespace: unbound
