apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: litellm
  namespace: litellm
spec:
  secretName: litellm-tls
  dnsNames:
    - llm.licht.moe
  issuerRef:
    name: acme-issuer
    kind: ClusterIssuer
