apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik
  namespace: authentik
spec:
  secretName: authentik-tls
  dnsNames:
    - authentik.licht.moe
  issuerRef:
    name: acme-issuer
    kind: ClusterIssuer
