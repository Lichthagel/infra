apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik
  namespace: authentik
spec:
  secretName: authentik-tls
  dnsNames:
    - authentik.licht
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
