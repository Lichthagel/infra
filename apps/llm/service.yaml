apiVersion: v1
kind: Service
metadata:
  name: llm
  namespace: llm
  labels:
    app: llm
spec:
  selector:
    app: llm
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
  type: ClusterIP
