apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: llm
  namespace: llm
spec:
  secretName: llm-tls
  dnsNames:
    - llm.licht
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
