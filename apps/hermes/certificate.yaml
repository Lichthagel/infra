apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hermes
  namespace: hermes
spec:
  secretName: hermes-tls
  dnsNames:
    - hermes.licht.moe
  issuerRef:
    name: acme-issuer
    kind: ClusterIssuer
