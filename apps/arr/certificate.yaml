apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: arr
  namespace: arr
spec:
  secretName: arr-tls
  dnsNames:
    - qbittorrent
    - prowlarr
    - sonarr
    - radarr
    - lidarr
    - bazarr
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
