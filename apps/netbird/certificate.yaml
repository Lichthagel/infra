apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: netbird
  namespace: netbird
spec:
  secretName: netbird-tls
  dnsNames:
    - netbird.licht.moe
  issuerRef:
    name: acme-issuer
    kind: ClusterIssuer
