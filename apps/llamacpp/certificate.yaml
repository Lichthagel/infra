apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: llamacpp
  namespace: llamacpp
spec:
  secretName: llamacpp-tls
  dnsNames:
    - llm.licht
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
